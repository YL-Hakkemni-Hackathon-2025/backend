# Environment Variables
NODE_ENV=development
PORT=3000

# MongoDB
MONGODB_URI=mongodb://localhost:27017/hakkemni

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# CORS
CORS_ORIGIN=http://localhost:3000

# App
APP_BASE_URL=https://hakkemni.health

# Google Cloud Document AI
# The SDK uses Application Default Credentials (ADC)
# Set this to your service account JSON file path
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your-service-account.json

# Alternatively, if running on Google Cloud (GCE, Cloud Run, etc.),
# the SDK will automatically use the instance's service account

# OpenAI (for document OCR and health pass AI recommendations)
OPENAI_API_KEY=your-openai-api-key

